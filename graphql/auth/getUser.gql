query getUser($id:uuid!) {
  users_by_pk(id: $id) {
    email
    firstName
    id
    lastName
    role
    bookings {
      id
      movie {
        title
        image {
          url
        }
      }
    }
  }
}
